# Backend environment variables for SQLite
# The backend uses a local SQLite database file
# No need to modify these unless you want to change the database location or port
DATABASE_URL=sqlite://data/mimikara_n3_questions.db
PORT=8081
